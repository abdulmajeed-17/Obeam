import { Injectable } from '@nestjs/common';

@Injectable()
export class AuthService {
  // TODO: signup, login, refresh, validateUser
  // - Hash password (bcrypt), create user + business, create NGN/GHS wallets
  // - JWT access + refresh tokens
}
